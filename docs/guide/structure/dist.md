# 📁 dist

Каталог собранного **фронтенд-приложения**, который генерируется автоматически при выполнении команд:

* `deploy-layout`
* `build-initial`

и попадает в `.gitignore`, чтобы не храниться в репозитории (каждый разработчик или CI/CD будет собирать проект сам).
